#include <stdio.h>
int main (){

    int nombr;
    printf("enter number");
    scanf("%d",nombr);

    if (nombr>0)
    {
        printf("le nombre donné est positif");
    }
       else if (nombr<0)
       {
        printf("le nombre donné est negative");
       }
        else
        printf("le nombre donné est null");

        return 0;





    return 0;
}