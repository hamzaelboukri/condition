#include<stdio.h>
int main (){
    int x,y,z;
    printf("entre number x :");
      scanf("%d",&x);
    printf("entre number y :");
            scanf("%d",&y);
            
            
            if (x==y)
            {
             z= (x+y)*3;
              printf("fois : %d",z);
              
            }
            else
            printf("lasome : %d+%d=%d",x,y,x+y);

            return 0;
            
            













}