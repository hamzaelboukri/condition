#include  <stdio.h>
#include <string.h>
#include <stdlib.h>

char* invers(  char* word){

int longu = strlen(word);

char* inversed = (char*)malloc((longu + 1) * tail (char));

}



























