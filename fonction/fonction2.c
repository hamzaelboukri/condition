#include <stdio.h>

int fois (int a,int b)
{
return a*b;

}
int main (){
    int a,b,x;
    printf("entre number a :");
     scanf ("%d",&a);
      printf("entre number b :");
     scanf ("%d",&b);


x=fois(a,b);

printf("%d",x);


}