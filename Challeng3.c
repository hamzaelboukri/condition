#include<stdio.h>
int main (){
    int x,y;
    printf("entre number x :");
      scanf("%d",&x);
    printf("entre number y :");
            scanf("%d",&y);
            
            
            if (x==y)
            {
              printf("fois : %d*%d=%d",x,y,x*y);
              
            }
            else
            printf("lasome : %d+%d=%d",x,y,x+y);

            return 0;
            
            













}