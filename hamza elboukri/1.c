#include <stdio.h>
int main (){
float hauteur,aire,base,perimetre;
  printf("entre base :");
        scanf("%f",&base);
  printf("entre base :");
        scanf("%f",&hauteur); 
       aire= (base*hauteur)/2;
        printf("(%.1f*%.1f)/2=%f\n",base,hauteur,aire);
        perimetre=3*base;
        printf("(3*%.1f)=%.1f",base,perimetre);



























return 0;

}